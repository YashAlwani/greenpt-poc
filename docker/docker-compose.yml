services:
  app:
    container_name: greenpt-json-optimizer
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - ../.env
    restart: unless-stopped
